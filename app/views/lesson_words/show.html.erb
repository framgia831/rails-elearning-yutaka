Category Title:<%= @lesson.category.title %><br>
Word content:<%= @lesson_word.word.content %><br>
Word Answers <% @lesson_word.word.word_answers.each do |word_answer| %>
<%= button_to   word_answer.content, lesson_lesson_word_path, :method => "put", params:{word_answer_id: word_answer.id}%>
<% end %>
